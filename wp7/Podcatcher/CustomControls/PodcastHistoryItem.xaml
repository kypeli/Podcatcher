<UserControl x:Class="Podcatcher.PodcastHistoryItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">
    
    <Grid x:Name="LayoutRoot">
        <StackPanel VerticalAlignment="Top">
            <StackPanel Orientation="Horizontal" Margin="12,0,0,0">
                <Image Source="{Binding PodcastSubscription.PodcastLogo}" Height="120" Width="120" HorizontalAlignment="Left"/>
                <TextBlock Text="{Binding EpisodeName}" TextWrapping="Wrap" Style="{StaticResource PhoneTextLargeStyle}" Margin="20,0,0,0" Width="318"/>
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>
